# Legacy Note

This file documented an older local run flow based on the legacy `server/` Node/Socket.io backend.

The current project uses:
- `client/` for the React frontend
- `worker/` for the Cloudflare Worker + Durable Object backend

Use these documents instead:
- `README.md` for the short start guide
- `project.md` for the full technical reference and current dev workflow
